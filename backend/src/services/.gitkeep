/**
 * Phazon Backend — Services Directory
 *
 * Business logic lives here — separate from controllers and routes.
 *
 * Planned service modules:
 *  - userService.js         (Step 24 — user CRUD)
 *  - courseService.js       (Step 24 — course & progress)
 *  - typingService.js       (Step 24 — typing results)
 *  - interviewService.js    (Step 24 — interview sessions)
 *  - badgeService.js        (Step 24 — badge awards)
 *  - certificateService.js  (Step 24 — cert issuance)
 *  - jobService.js          (Step 24 — job recommendations)
 *  - aiService.js           (Step 26 — Gemini proxy)
 */

'use strict';

// No active services yet — this file documents the planned structure.
